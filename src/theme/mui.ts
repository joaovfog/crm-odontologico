import { createTheme } from '@mui/material'

export const muiTheme = createTheme({
    typography: {
        fontFamily: 'Arial',
        fontSize: 14
    },
    palette: {
        primary: {
            main: '#6eb6ff'
        }
    }
})